<UserControl x:Class="Frank_UI.mListTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:loc="clr-namespace:Frank_UI"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <ListView HorizontalAlignment="Stretch" Height="22" VerticalAlignment="Top">
            <ListView.View>
                <GridView x:Name="gvColumn">
                    <GridViewColumn Header="Nummer" Width="100" DisplayMemberBinding="{Binding Nummer}" x:Name="gclmNummer"/>
                    <GridViewColumn Header="Liefertermin" DisplayMemberBinding="{Binding Liefertermin}" x:Name="gclmLiefertermin"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TreeView Name="trv" Margin="0, 22, 0, 0" SelectedItemChanged="trv_SelectedItemChanged">
        </TreeView>
    </Grid>
</UserControl>
